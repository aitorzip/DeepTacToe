#ifndef AB_STATE_GRID_H
#define AB_STATE_GRID_H

// DeepTacToeGame - Proprietary and confidential
// StateGrid Aitor Ruano
// Copyright (C) Autobon.ai, LLC - All Rights Reserved
// Unauthorized copying of this file via any medium
// is strictly prohibited without the written permission

#include <cstdint>
#include <vector>

/*
class StateGrid
{
    public:
        typedef std::vector<uint8_t> TVec;

         StateGrid(uint8_t n_states, uint8_t size);
        ~StateGrid(void);

        uint8_t getStateAt(uint8_t idx) const { return _stateGrid[idx]; }
        void    setStateAt(uint8_t idx, uint8_t state);
        uint8_t getOverallState(uint8_t** valid_combos);

    private:
        uint8_t  _states;
        TVec     _stateGrid;
};
*/

#endif // AB_STATE_GRID_H
